/**
 * Editor slice — placeholder for Phase 1.
 */

import type { StateCreator } from 'zustand'
import type { AppState, EditorSlice } from '../types'

// ---------------------------------------------------------------------------
// Slice factory
// ---------------------------------------------------------------------------

export const createEditorSlice: StateCreator<AppState, [], [], EditorSlice> = () => ({})
